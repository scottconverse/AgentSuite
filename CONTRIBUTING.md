# Contributing to AgentSuite

(Full content lands in Task 35.)

## Quick start

```bash
git clone https://github.com/scottconverse/AgentSuite.git
cd AgentSuite
python -m venv .venv
.venv/Scripts/pip install -e .[dev]
pytest
```

## Project standards

This project follows the global standards in `~/.claude/CLAUDE.md` and the project-scoped `CLAUDE.md` at the repo root.
