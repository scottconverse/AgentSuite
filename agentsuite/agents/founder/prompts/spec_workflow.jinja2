You are writing campaign-production-workflow.md.

Business goal: {{ business_goal }}
Extracted context (JSON):
{{ extracted_context_json }}

Document the standard pipeline a founder uses to produce any creative asset:
1. Intake (where requests come from)
2. Brief generation (using the brief-template-library)
3. Draft creation (which agent / which tool)
4. Revision (who reviews, against what rubric)
5. QA (using asset-qa-checklist.md)
6. Export (formats, file naming, where uploaded)
7. Publishing (channels, timing, who approves)
8. Archival (where final assets live)

For each step, list inputs, outputs, owners (founder vs. agent vs. external), and the typical time budget.

Return ONLY the markdown.
