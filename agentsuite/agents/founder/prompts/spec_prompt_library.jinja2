You are writing reusable-prompt-library.md.

Business goal: {{ business_goal }}
Extracted context (JSON):
{{ extracted_context_json }}

For each common asset type (landing hero copy, social post, email, slide title, press pitch hook, demo script intro, comparison table copy), produce a reusable prompt template with placeholders:
- {audience}
- {claim}
- {proof}
- {visual_metaphor}
- {tone_descriptor}
- {format_constraint}

Each template should be specific enough that filling the placeholders produces a controlled output, not a generic SaaS sentence.

Return ONLY the markdown.
