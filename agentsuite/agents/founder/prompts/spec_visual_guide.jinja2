You are writing visual-style-guide.md.

Business goal: {{ business_goal }}
Extracted context (JSON):
{{ extracted_context_json }}

Sections:
1. Color palette (primary, secondary, neutrals) — list hex codes with named roles
2. Typography (display + body) — pair recommendations + rationale
3. Photography / imagery style — what kind of imagery fits, what doesn't
4. Iconography — line vs. solid, weight, where used
5. Spacing and density — guidance on whitespace
6. Motion — subtle, emphatic, or none
7. Anti-patterns — visual cliches we never use (stock-photo handshakes, neon gradients, etc.)

If insufficient signal exists in extracted context, mark sections "INFERRED FROM POSITIONING" and recommend explicit human input.

Return ONLY the markdown.
