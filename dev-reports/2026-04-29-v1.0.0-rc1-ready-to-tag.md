# v1.0.0-rc1 — Ready to tag (awaiting Scott approval)

**Date:** 2026-04-29
**Branch:** `main` at `1509cd2` (3 commits ahead of `v0.9.3` / `309b06d`)

## What done — the rc1 stack

| Commit | Purpose |
|---|---|
| `39feb6b` | rc1-prep 1/N — `py.typed` marker + downstream-consumer mypy test + README "Why" hook + test-coverage audit-honesty pass |
| `dc15ee6` | rc1-prep 2/N — `docs/community/discussions-seeds.md` + `docs/community/good-first-issues.md` drafts |
| `1509cd2` | release — bump 0.9.3 → 1.0.0rc1, promote CHANGELOG with Compatibility-freeze + Known-limitations sections |

## Sprint-plan rc1 acceptance — line by line

| Plan item | Status |
|---|---|
| 1. Compatibility freeze (public API + state schema + MCP names + pipeline locked) | **Done.** `py.typed` shipped; downstream-consumer mypy test passing; CHANGELOG records the locked surface. |
| 2. Discussions seeded | **Drafts ready** in `docs/community/discussions-seeds.md`. Posting requires Discussions enabled in repo Settings (manual UI step). |
| 3. "Why AgentSuite" hook in README | **Done.** Tightened pre-install paragraph: target user + 30-sec pitch + differentiation. |
| 4. Three good-first-issue tickets filed | **Drafts ready** in `docs/community/good-first-issues.md`. Filing requires `gh issue create` with maintainer approval. |
| 5. CHANGELOG promotion to 1.0.0-rc1 | **Done** in `1509cd2`. |
| 6. Internal dogfood (5–7 days, real-project) | **Cannot do for you.** This is the bake step — needs your hands-on time against a real Scott project. |

Items 2 and 4 are review-ready content waiting for the manual GitHub side-effects (enable Discussions, post seeds, file issues). Item 6 is the bake clock that starts the day rc1 ships.

## Verified

- `bash scripts/verify-release.sh` → **ALL CHECKS PASSED** on `1509cd2`. All 8 gates green (doc artifacts, version sync, CHANGELOG entry, ruff, mypy, pytest, cleanroom, build, secrets scan).
- Full pytest: **689 passed, 3 deselected, 0 skipped, 0 errors** (was 688/691 at v0.9.3; +1 test from `test_downstream_consumer.py`).
- Hard Rule 11 commit-size: rc1 release commit is 187 lines (well under 800). No tag needed.
- Hard Rule 4a: zero `pytest.skip()` calls and zero unconditional `@pytest.mark.skip` markers. The one `@pytest.mark.skipif` for cassette recording is documented in `docs/test-coverage.md` with rationale.
- Hard Rule 9: all 6 doc artifacts present (README, CHANGELOG, CONTRIBUTING, LICENSE, .gitignore, docs/index.html).
- All version pins consistent at `1.0.0rc1` (PEP 440 form on package, `v1.0.0-rc1` form in human-readable copy).

## Compat-freeze claims — sanity check

The CHANGELOG promises these are locked:

- Public API: `<Agent>Agent`, `<Agent>AgentInput` for all 7 agents → **all exist**, all imported in `test_downstream_consumer.py` and pass strict mypy.
- `RunState`, `Constraints`, `ArtifactRef`, `Cost`, `Stage` → exist in `agentsuite.kernel.schema`.
- `QARubric`, `RubricDimension` → exist in `agentsuite.kernel.qa`.
- `LLMProvider`, `MockLLMProvider` → exist in `agentsuite.llm.{base,mock}`.
- `_state.json schema_version: 2` → enforced by `RunStateSchemaVersionError` (per ADR-0002, ADR-0007).
- MCP tool names `<agent>_run|resume|approve` → per ADR-0004.
- Stage names `intake → extract → spec → execute → qa → approval` → per `agentsuite.kernel.schema.Stage`.

Every claim is grounded in shipped code referenced from tests or ADRs.

## Not verified

- **Dogfood bake** — by definition not done before rc1 ships. The 5–7 day clock starts with this tag.
- **Lighthouse pass on `docs/index.html`** — sprint plan mentions it; not run this session. Worth a manual run before GA.
- **CI on rc1 tag push** — pending tag.

## Broken

Nothing.

## Open Scott decisions for rc1

1. **GPG-signed tags from rc1 onward?** Sprint plan recommends yes for v1.0 launch credibility. ~20–30 min one-time GPG setup. If yes, configure before tagging rc1 (otherwise rc1 itself stays unsigned and the first signed tag is v1.0.0 GA).
2. **Pre-release flag on the GH release** — yes (rc1 is by definition pre-release). Confirmed in the suggested commands below.
3. **Manual side-effects you'll do after the tag pushes:**
   - Repo Settings → Discussions → enable
   - Paste the 5 seed posts from `docs/community/discussions-seeds.md` into the appropriate categories
   - File 3 issues from `docs/community/good-first-issues.md` via `gh issue create`
   - Mark the rc1 GH release as pre-release

## Approve push?

```bash
cd /c/Users/scott/OneDrive/Desktop/Claude/AgentSuite
# If GPG signing is wanted, set it up first:
#   git config user.signingkey <KEY-ID>
#   git config commit.gpgsign true
# then add `-s` to the tag command below.

git tag v1.0.0-rc1 -m "v1.0.0-rc1 — Release candidate: compatibility freeze, community drafts, py.typed"
git push origin main
git push origin v1.0.0-rc1

# After CI green:
gh release edit v1.0.0-rc1 --prerelease
gh release view v1.0.0-rc1 --json assets,isPrerelease
```

After rc1 push, the bake-clock starts. Plan to surface v1.0.0 GA tag candidate after 5–7 days of dogfood unless you want to proceed sooner.
