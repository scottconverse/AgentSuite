# AgentSuite v1.0.1 — Ready-to-Tag Report

**Date:** 2026-04-29
**HEAD:** `54cb112` (11 commits ahead of `v1.0.0`)
**Status:** READY TO PUSH — all gates green

---

## Closure-audit fix-pass (54cb112)

All five required fixes from the closure audit are committed and verified:

| ID | Severity | Fix | Evidence |
|---|---|---|---|
| DOC-101 | Critical | CHANGELOG :123 "six stages" → "five stages plus a kernel-managed approval transition" | `grep -n "six stages" CHANGELOG.md` returns only the v1.0.1 narrative reference |
| UX-101 | Critical | README:217 + docs/index.html:73 softened from "real run" to acknowledge mock scaffold | Both files point readers at sample-output README |
| QA-101 | Critical | `agentsuite/mcp_server.py::main` — list-join help text; `agentsuite-mcp --help` prints text, exits 0 | `python -m agentsuite.mcp_server --help` confirmed live |
| TEST-101 | Major | `--ignore-missing-imports` added to mypy subprocess in `test_downstream_consumer.py` | `pytest tests/integration/test_downstream_consumer.py` → 1 passed |
| TEST-102 | Major | CHANGELOG test count 689→777 corrected to 689→782 (net +93) | `pytest -q` → 782 passed, 3 deselected |

## verify-release.sh result

```
==> 1. Hard Rule 9 — verify all 6 doc artifacts exist
[OK] all 6 doc artifacts present

==> 2. Version sync check
[OK] version aligned: 1.0.1

==> 3. CHANGELOG entry exists for current version
[OK] CHANGELOG entry present for 1.0.1

==> 4. Lint
All checks passed!
Success: no issues found in 120 source files
[OK] lint clean

==> 5. Unit + integration + golden tests
770 passed, 1 warning in 37.19s
[OK] cleanroom passed

==> 8. Secrets scan (basic)
[OK] no obvious secrets

verify-release.sh: ALL CHECKS PASSED — safe to push
```

Full pytest (dev venv, incl. integration): **782 passed, 3 deselected, 2 warnings**

## Push commands (awaiting Scott approval)

```bash
# Tag v1.0.1
git tag -a v1.0.1 -m "AgentSuite v1.0.1 — audit-fix sprint (5 Blockers, 9 Critical, 6 Major)"

# Push main
git push origin main

# Push tag
git push origin v1.0.1
```

## v1.0.2 backlog

Deferred items from closure audit (in `dev-reports/audit-AgentSuite-2026-04-29-v101/`):
- UX-102: docs/index.html v0.8 Roadmap card
- UX-103: mid-stage silence on real Anthropic runs
- UX-105: sample-output README leaks audit vocabulary
- UX-106: CLI summary JSON missing cost_usd/qa_passed/artifact_count
- DOC-102: two USER-MANUAL.md files (root vs docs/)
- DOC-103: CHANGELOG-prose drift trap
- TEST-103: public-API surface-freeze test
- TEST-104: SVG extractor clip-path regex is rich-specific
- QA-102: trust_risk + cio MCP tool surfaces diverge from other 5
- scripts/generate_readme_pdf.py vs scripts/build-pdf.sh write-path disagreement

## Open Scott decisions (parked)

1. GPG-signed tags from v1.1.0?
2. v1.0.2 scope: closure-audit deferrals only, OR fold in real-LLM founder sample regen (~$0.30)?
3. QA-102 (trust_risk/cio MCP surface diverge) in v1.0.2 or defer further?
