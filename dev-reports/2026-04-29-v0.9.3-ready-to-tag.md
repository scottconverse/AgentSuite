# v0.9.3 — Ready to tag (awaiting Scott approval)

**Date:** 2026-04-29
**Branch:** `main` at `309b06d` (2 commits ahead of `v0.9.2` / `670bbd2`)

## What done

| Commit | Item |
|---|---|
| `9c23785` | `[LARGE-CHANGE]` feat(docs): committed sample-output + 5 SVG screenshots |
| `309b06d` | chore: release v0.9.3 — wire screenshots into README + index.html, bump version, promote CHANGELOG |

### Sample-output (browsable on GitHub, no install)
- `examples/sample-output/founder/` — 30 files (29 artifacts + README): full deterministic Founder run for the PatentForgeLocal scenario.
- Generated under `agentsuite.llm.mock._default_mock_for_cli`. Bytes stable run-to-run.
- README in the directory explains origin and reproduction.

### Screenshots (5 of 6)
- `docs/screenshots/cli-founder-run.svg` — synthetic terminal session showing the CLI invocation, 5 stage checkmarks, and JSON summary with `cost_usd`/`qa_passed`/`artifact_count`.
- `docs/screenshots/runs-tree.svg` — `tree .agentsuite/runs/<run-id>/` rendered from the actual sample-output dir listing.
- `docs/screenshots/brand-system-rendered.svg` — `brand-system.md` rendered as terminal-styled markdown.
- `docs/screenshots/qa-report-rendered.svg` — `qa_report.md` rendered as terminal-styled markdown.
- `docs/screenshots/kernel-tree.svg` — synthetic approval flow + `_kernel/<project_slug>/` tree.
- All 5 generated programmatically via `rich.Console.save_svg()` (Fira Code embedded; renders in any modern browser).

### Wire-up
- README hero screenshot (`cli-founder-run.svg`) above the install block.
- README "Screenshots and sample output" section with all 5 SVGs + sample-output link.
- `docs/index.html` sample section now embeds the 5 SVGs in place of the previous text-only mock output.
- Version 0.9.2 → 0.9.3 across pyproject.toml, agentsuite/__version__.py, README, docs/index.html, docs/troubleshooting.md (×2).
- CHANGELOG `[Unreleased]` promoted to `[0.9.3] - 2026-04-29` with Added + Deferred subsections.

### Reproduction
- `scripts/_v093_wire.py` — one-shot wire-up script kept in repo for documentation. SVG generation logic was inline in the session and is referenced in the v0.9.3 dev report.

## Verified

- `bash scripts/verify-release.sh` → **ALL CHECKS PASSED** (run on `309b06d`).
- All 6 doc artifacts present (Hard Rule 9): README, CHANGELOG, CONTRIBUTING, LICENSE, .gitignore, docs/index.html.
- All version pins consistent at 0.9.3.
- Hard Rule 11 commit-size: `9c23785` is 1591 lines, prefixed with `[LARGE-CHANGE]` per the rule.

## Not verified

- Visual fidelity of SVGs in browser — they render valid SVG (rich's standard template, used widely; identical mechanism to `rich.console.export_svg()` examples in the upstream docs). Scott may want to spot-check one in a browser.
- Lighthouse pass on `docs/index.html` — sprint plan mentions this; not run this session. Will be re-checked at v1.0.0-rc1.
- 6th screenshot (`mcp-tool-list-claude-code.png`) — deferred to manual capture per the sprint plan; documented in CHANGELOG under Deferred.

## Broken

Nothing.

## Next decision needed (Scott)

**Approve tag + push?**

```bash
cd /c/Users/scott/OneDrive/Desktop/Claude/AgentSuite
git tag v0.9.3 -m "v0.9.3 — Visual + sample-output (P4): screenshots + browsable example run"
git push origin main
git push origin v0.9.3
gh run list --limit 3
```

Three open Scott decisions still parked (NOT v0.9.3 blockers; needed for v1.0.0-rc1):
1. GPG-signed tags from v1.0?
2. Three good-first-issue ticket topics for rc1?
3. MCP screenshot capture session — once you've got a Claude Code window open with AgentSuite registered, screenshot the tool list and drop it at `docs/screenshots/mcp-tool-list-claude-code.png`. I'll wire it into README + index.html.
