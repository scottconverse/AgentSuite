# v0.9.2 — Ready to tag (awaiting Scott approval)

**Date:** 2026-04-28
**Branch:** `main` at `f5c9037` (4 commits ahead of `v0.9.1` / `b5a2b59`)

## What done

All three v0.9.2 sprint items + version bump committed locally, awaiting tag + push approval.

| Commit | Item |
|---|---|
| `c0cf1e6` | docs(rubric): Founder rubric audit one-pager (v0.9.2 1/3) |
| `d555a76` | test(golden): content-aware coverage for remaining 6 agents (v0.9.2 2/3) |
| `7e52440` | docs(testing): document deselected pytest markers (v0.9.2 3/3) |
| `f5c9037` | chore: release v0.9.2 — sprint 3 cleanup |

### Item 1 — Rubric audit one-pager
- New `docs/rubric-audit.md` (102 lines): cross-reference of 63 dimensions across 7 agents grouped by 26 themes.
- Headline: all 7 agents already at 9 dimensions (commit `2b1dda0` bumped Founder from 7 to 9). ADR-0001 narrative refreshed; decision intact. Links from ADR-0001 + CONTRIBUTING.

### Item 2 — Content-snapshot pattern roll-out
- 6 new agent snapshots: `tests/golden/snapshots/<agent>/<scenario>/<primary>.md` + `qa_scores.json`.
- Primary specs: visual-direction.md (Design), product-requirements-doc.md (Product), architecture-decision-record.md (Engineering), campaign-brief.md (Marketing), threat-model.md (TrustRisk), it-strategy.md (CIO).
- 12 new tests (2 per agent) using `assert_artifact_exact` + `assert_qa_within_tolerance(rtol=0.05)`.
- Golden suite: 42 → 54 tests, all green.

### Item 3 — Test-coverage doc
- New `docs/test-coverage.md` documents the 3 marker-gated tests (`cleanroom`, `live`, `live_ollama`).
- Hard Rule 4a confirmed: zero `pytest.skip` in repo; gated ≠ skipped.
- 688 of 691 tests in default invocation; 3 deselected by config.

### Item 4 — Version bump
- 0.9.1 → 0.9.2 in pyproject.toml, agentsuite/__version__.py, README.md, docs/index.html, docs/troubleshooting.md (×2).
- CHANGELOG `[Unreleased]` block promoted to `[0.9.2] — 2026-04-28`.

## Verified

- `bash scripts/verify-release.sh` → **ALL CHECKS PASSED** (run twice during the sprint, final run on the version-bump commit).
- `pytest tests/golden -v` → 54 passed.
- `git log` shows 4 clean atomic commits on top of `v0.9.1`.
- All 6 doc artifacts present (Hard Rule 9 gate green): README, CHANGELOG, CONTRIBUTING, LICENSE, .gitignore, docs/index.html.
- All version pins consistent at 0.9.2.

## Not verified

- Full `pytest` (default suite, ~688 tests) — only golden subset run this session. verify-release.sh runs the full suite as part of step 5; that ran green.
- Live tier (`RUN_LIVE_TESTS=1`) — per project policy live tests run only at `v0.X.0` tags. v0.9.2 is a patch release. No live run needed.
- CI on tag push — pending tag.

## Broken

Nothing. Commit `f5c9037` exceeded the Hard Rule 11 800-line threshold (1091 lines incl. previously untracked `dev-reports/` documentation). The commit-size gate did not block — either deactivated in this workspace or excluding `dev-reports/`. Flagging because the standard says to tag with `[LARGE-CHANGE]` if hit; if Scott wants the commit retro-tagged via amend, surface and ask. Otherwise no impact — content is documentation only.

## Next decision needed (Scott)

**Approve tag + push?** Local commits are ready. Suggested commands:

```bash
cd /c/Users/scott/OneDrive/Desktop/Claude/AgentSuite
git tag v0.9.2 -m "v0.9.2 — Sprint 3 cleanup: rubric audit + golden roll-out + test coverage doc"
git push origin main
git push origin v0.9.2
gh run list --limit 3
```

After CI green:

```bash
gh release view v0.9.2 --json assets --jq '.assets[].name'
```

Three open Scott decisions still parked (NOT v0.9.2 blockers; needed for v1.0.0-rc1):
1. GPG-signed tags from v1.0?
2. Three good-first-issue ticket topics for rc1?
3. v0.9.3 screenshot automation approach?

Loop terminator unchanged: v1.0.0 GA. After v0.9.2 ships, next sprint is v0.9.3 (screenshots + sample-output).
